<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.xlj.modules.orderdetail.dao.OrderDetailDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.xlj.modules.orderdetail.entity.OrderDetailEntity" id="orderDetailMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="content" column="content"/>
        <result property="createdTime" column="created_time"/>
        <result property="createdUser" column="created_user"/>
        <result property="updatedTime" column="updated_time"/>
        <result property="updatedUser" column="updated_user"/>
    </resultMap>

	<select id="queryObject" resultType="cn.xlj.modules.orderdetail.entity.OrderDetailEntity">
		select * from tb_order_detail where id = #{value}
	</select>

	<select id="queryList" resultType="cn.xlj.modules.orderdetail.entity.OrderDetailEntity">
		select * from tb_order_detail
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from tb_order_detail 
	</select>
	 
	<insert id="save" parameterType="cn.xlj.modules.orderdetail.entity.OrderDetailEntity" useGeneratedKeys="true" keyProperty="id">
		insert into tb_order_detail
		(
			`name`, 
			`content`, 
			`created_time`, 
			`created_user`, 
			`updated_time`, 
			`updated_user`
		)
		values
		(
			#{name}, 
			#{content}, 
			#{createdTime}, 
			#{createdUser}, 
			#{updatedTime}, 
			#{updatedUser}
		)
	</insert>
	 
	<update id="update" parameterType="cn.xlj.modules.orderdetail.entity.OrderDetailEntity">
		update tb_order_detail 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="content != null">`content` = #{content}, </if>
			<if test="createdTime != null">`created_time` = #{createdTime}, </if>
			<if test="createdUser != null">`created_user` = #{createdUser}, </if>
			<if test="updatedTime != null">`updated_time` = #{updatedTime}, </if>
			<if test="updatedUser != null">`updated_user` = #{updatedUser}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from tb_order_detail where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from tb_order_detail where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>